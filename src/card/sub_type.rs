#[derive(Debug, PartialEq, Eq, Hash, Clone, Copy)]
#[allow(dead_code)]
pub enum SubType {
    Attraction,
    Blood,
    Bobblehead,
    Clue,
    Contraption,
    // TODO: quite a lot of these to add.
}
